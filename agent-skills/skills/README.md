# Skills

Each skill should be a small, composable playbook with a `SKILL.md`, optional `references/`, optional helper `scripts/`, and optional `assets/`.

The skill catalog is `catalog.yaml`.

Validate the current skill set with:

```bash
ruby agent-skills/scripts/validate-skills.rb
```
