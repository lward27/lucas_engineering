grafana:
  service:
    type: ClusterIP
  persistence:
    enabled: true
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana.lucas.engineering
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi