grafana:
  service:
    type: ClusterIP
  persistence:
    enabled: true
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - grafana.lucas.engineering
  resources:
    limits:
      cpu: 1000m
      memory: 2048Mi
    requests:
      cpu: 500m
      memory: 1024Mi