---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: hermes-no-secrets-read-note
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
    agentic.lucas.engineering/note: Kubernetes RBAC is additive, so this role intentionally has no rules and documents that Hermes phase 1 roles do not grant secret reads.
  labels:
{{ include "agentic-platform.labels" . | indent 4 }}
    agentic.lucas.engineering/role: guardrail
rules: []
