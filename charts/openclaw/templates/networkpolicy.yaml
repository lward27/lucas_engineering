{{- if .Values.openclaw.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.openclaw.name }}-custom
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.openclaw.name }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ingress-nginx
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: {{ .Release.Namespace }}
  egress:
  # DNS
  - ports:
    - port: 53
      protocol: UDP
    - port: 53
      protocol: TCP
  # HTTPS (external APIs - Fireworks AI, etc.)
  - ports:
    - port: 443
      protocol: TCP
  # SSH to homelab VMs
  - to:
    - ipBlock:
        cidr: 192.168.20.0/24
    ports:
    - port: 22
      protocol: TCP
  # Kubernetes API
  - ports:
    - port: 6443
      protocol: TCP
  # OpenTelemetry Collector (monitoring namespace) - OTLP gRPC + HTTP
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: monitoring
    ports:
    - port: 4317
      protocol: TCP
    - port: 4318
      protocol: TCP
{{- end }}
