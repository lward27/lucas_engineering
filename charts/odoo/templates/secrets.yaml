{{- /*
Create the Odoo credentials outside this chart before syncing the app.

Expected admin secret:
  name: odoo-auth
  key: odoo-password

Expected PostgreSQL secret:
  name: odoo-postgresql-auth
  keys:
    password
    postgres-password

Example:
  kubectl -n odoo create secret generic odoo-auth \
    --from-literal=odoo-password='REPLACE_ME'

  kubectl -n odoo create secret generic odoo-postgresql-auth \
    --from-literal=password='REPLACE_ME' \
    --from-literal=postgres-password='REPLACE_ME'
*/ -}}
