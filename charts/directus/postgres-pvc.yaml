apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: directus-postgres-data
  namespace: directus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
