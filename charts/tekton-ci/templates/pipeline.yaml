apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: clone-build-push
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/part-of: tekton-ci
spec:
  description: |
    Clone a git repo, build through the isolated desktop BuildKit, and push to the registry.
    Reused by all microservice triggers via parameterized TriggerTemplates.
  params:
    - name: repo-url
      type: string
      description: The git repo URL to clone from.
    - name: revision
      type: string
      description: Git branch, tag, SHA, or ref to build.
      default: ""
    - name: image-reference
      type: string
      description: Full image reference to build and push (registry/name:tag).
    - name: dockerfile
      type: string
      description: Path to the Dockerfile relative to the repo root.
      default: "./Dockerfile"
    - name: context
      type: string
      description: Build context directory relative to the repo root.
      default: "./"
    - name: build-args
      type: array
      description: Dockerfile build arguments in NAME=value form.
      default:
        {{- range .Values.registry.buildArgs }}
        - {{ . | quote }}
        {{- end }}
    - name: deployment
      type: string
      description: Deployment name to restart after build (empty = skip).
      default: ""
    - name: deployment-namespace
      type: string
      description: Namespace of the deployment to restart.
      default: "apps-prod"
  workspaces:
    - name: shared-data
      description: Shared workspace between git-clone and image-build tasks.
  tasks:
    - name: fetch-source
      taskRef:
        name: git-clone
      workspaces:
        - name: output
          workspace: shared-data
      params:
        - name: url
          value: $(params.repo-url)
        - name: revision
          value: $(params.revision)

    - name: build-push
      runAfter:
        - fetch-source
      taskRef:
        name: remote-buildkit
      workspaces:
        - name: source
          workspace: shared-data
      params:
        - name: IMAGE
          value: $(params.image-reference)
        - name: DOCKERFILE
          value: $(params.dockerfile)
        - name: CONTEXT
          value: $(params.context)
        - name: BUILD_ARGS
          value: $(params.build-args[*])

    - name: rollout-restart
      runAfter:
        - build-push
      when:
        - input: $(params.deployment)
          operator: notin
          values: [""]
      taskRef:
        name: rollout-restart
      params:
        - name: DEPLOYMENT
          value: $(params.deployment)
        - name: NAMESPACE
          value: $(params.deployment-namespace)
