{{- if (index .Values.apps "agentic-platform").enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentic-platform
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/lward27/lucas_engineering.git
    path: charts/agentic-platform
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: agent-runtime
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
{{- end }}
