{{- if (index .Values.apps "nanoclaw").enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nanoclaw
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/lward27/lucas_engineering.git
    path: charts/nanoclaw
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: nanoclaw
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
{{- end }}
