{{- if (index .Values.apps "epheros").enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: epheros
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/lward27/lucas_engineering.git
    path: charts/epheros
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: epheros
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
{{- end }}
