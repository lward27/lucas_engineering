{{- if (index .Values.apps "forge-admin").enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: forge-admin
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/lward27/forge.git
    path: charts/forge-admin
    targetRevision: HEAD
    helm:
      valuesObject:
          forgeAdmin:
            ingress:
              annotations:
                cert-manager.io/private-key-rotation-policy: "Never"
                cert-manager.io/revision-history-limit: "2147483647"
  destination:
    server: https://kubernetes.default.svc
    namespace: forge-platform
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - CreateNamespace=true
{{- end }}
