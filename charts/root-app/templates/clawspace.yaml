{{- if (index .Values.apps "clawspace").enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clawspace
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/lward27/lucas_engineering.git
    path: charts/clawspace
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: apps-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
{{- end }}
