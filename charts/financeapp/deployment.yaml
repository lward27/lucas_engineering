apiVersion: v1
kind: Service
metadata:
  name: financeapp
spec:
  selector:
    app: financeapp
  ports:
  - protocol: "TCP"
    port: 8090
    targetPort: 8000
  type: LoadBalancer

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: financeapp
spec:
  selector:
    matchLabels:
      app: financeapp
  replicas: 1
  template:
    metadata:
      labels:
        app: financeapp

    spec:
      containers:
      - name: financeapp
        image: docker.lucas.engineering/financeapp:0.3
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000